name: cli

on:
  push:
    paths-ignore:
      - cli

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

  windows:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

  linux:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

  darwin:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

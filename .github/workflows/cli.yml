name: cli

on:
  workflow_dispatch:
  pull_request:
  push:
    tags:
      - '*'

permissions:
  # Allow checks read
  checks: write
  # Allow repo checkout
  contents: write
  # Allow PRs read
  pull-requests: read

env:
  GRAFBASE_SKIP_ASSET_VERSION_CHECK: 'true'
  ASSETS_VERSION: 'release/23717dd-2023-04-28'
  PROD_ASSETS: assets.grafbase.com
  CARGO_TERM_COLOR: 'always'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  detect-change-type:
    runs-on: ubuntu-latest-8-cores
    outputs:
      build: ${{ steps.filter.outputs.build }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Path filter check
        uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            build:
              - 'cli/**/*'
              - '.github/**/*'

  lint:
    needs: [detect-change-type]
    if: ${{ needs.detect-change-type.outputs.build == 'true' || startsWith(github.ref, 'refs/tags/') }}
    runs-on: ubuntu-latest-8-cores
    env:
      MOONBASE_SECRET_KEY: ${{ secrets.MOONBASE_SECRET_KEY }}
      MOONBASE_ACCESS_KEY: ${{ secrets.MOONBASE_ACCESS_KEY }}
    steps:
      - name: Get sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Rust job init
        uses: ./.github/actions/init_rust_job
        with:
          platform: linux

      - name: Fetch CDN assets
        uses: ./.github/actions/cdn_assets
        with:
          cdn: ${{ env.PROD_ASSETS }}
          assets_version: ${{ env.ASSETS_VERSION }}

      - name: Sanitize
        uses: ./.github/actions/sanitize
        with:
          test: false

      - name: Individual package build
        shell: bash
        run: |
          set -euo pipefail
          # The actual features used for each dependency depends on what is being built simultaneously.
          # However, each package is published individually, so we're checking that all packages compile
          # with only their defined features.
          # See: https://github.com/rust-lang/cargo/issues/4463
          moon run --concurrency 1 :check

  windows:
    needs: [lint]
    runs-on: windows-latest-8-cores
    env:
      MOONBASE_SECRET_KEY: ${{ secrets.MOONBASE_SECRET_KEY }}
      MOONBASE_ACCESS_KEY: ${{ secrets.MOONBASE_ACCESS_KEY }}
    steps:
      - name: Get sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Rust job init
        uses: ./.github/actions/init_rust_job
        with:
          platform: windows

      - name: Fetch CDN assets
        uses: ./.github/actions/cdn_assets
        with:
          cdn: ${{ env.PROD_ASSETS }}
          assets_version: ${{ env.ASSETS_VERSION }}

      - if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        name: Sanitize
        uses: ./.github/actions/sanitize
        with:
          lint: false

      - name: Build release
        run: |
          cd cli
          moon run --concurrency 1 :build -- --release --target x86_64-pc-windows-msvc

      - if: startsWith(github.ref, 'refs/tags/')
        name: Upload binaries
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.ref_name }}-windows
          path: |
            cli/target/x86_64-pc-windows-msvc/release/grafbase.exe

  linux:
    needs: [lint]
    runs-on: ubuntu-latest-8-cores
    env:
      MOONBASE_SECRET_KEY: ${{ secrets.MOONBASE_SECRET_KEY }}
      MOONBASE_ACCESS_KEY: ${{ secrets.MOONBASE_ACCESS_KEY }}
    steps:
      - name: Get sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Rust job init
        uses: ./.github/actions/init_rust_job
        with:
          platform: linux

      - name: Fetch CDN assets
        uses: ./.github/actions/cdn_assets
        with:
          cdn: ${{ env.PROD_ASSETS }}
          assets_version: ${{ env.ASSETS_VERSION }}

      - if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        name: Sanitize
        uses: ./.github/actions/sanitize
        with:
          lint: false

      - name: Build release
        run: |
          cd cli
          moon run --concurrency 1 :build -- --release --target x86_64-unknown-linux-musl

      - if: startsWith(github.ref, 'refs/tags/')
        name: Upload binaries
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.ref_name }}-linux
          path: |
            cli/target/x86_64-unknown-linux-musl/release/grafbase

  darwin:
    needs: [lint]
    runs-on: macos-latest
    env:
      MOONBASE_SECRET_KEY: ${{ secrets.MOONBASE_SECRET_KEY }}
      MOONBASE_ACCESS_KEY: ${{ secrets.MOONBASE_ACCESS_KEY }}
    strategy:
      fail-fast: false
      matrix:
        target: [aarch64-apple-darwin, x86_64-apple-darwin]
    steps:
      - name: Get sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Rust job init
        uses: ./.github/actions/init_rust_job
        with:
          platform: macos

      - name: Fetch CDN assets
        uses: ./.github/actions/cdn_assets
        with:
          cdn: ${{ env.PROD_ASSETS }}
          assets_version: ${{ env.ASSETS_VERSION }}

      - if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        name: Sanitize
        uses: ./.github/actions/sanitize
        with:
          lint: false

      - name: Build ${{ matrix.target }} release
        run: |
          cd cli
          moon run --concurrency 1 :build -- --release --target ${{ matrix.target }}

      - if: startsWith(github.ref, 'refs/tags/')
        name: Upload ${{ matrix.target }} binary
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.ref_name }}-${{ matrix.target }}
          path: cli/target/${{ matrix.target }}/release/grafbase

  release:
    needs: [windows, linux, darwin]
    if: startsWith(github.ref, 'refs/tags/')
    uses: ./.github/workflows/cli-partial-release.yml
    with:
      draft: false
      tag: ${{ github.ref_name }}
      prerelease: ${{ contains(github.ref, 'beta') || contains(github.ref, 'alpha') || contains(github.ref, 'rc') }}
    secrets:
      CRATES_ACCESS_TOKEN: ${{ secrets.CRATES_ACCESS_TOKEN }}
      NPM_ACCESS_TOKEN: ${{ secrets.NPM_ACCESS_TOKEN }}

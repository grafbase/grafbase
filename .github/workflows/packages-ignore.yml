name: packages

on:
  push:
    paths-ignore:
      - 'packages/**'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Skipping CI"'

[package]
name = "dynamodb-utils"
authors = ["Grafbase"]
description = "Various utilities for use with DynamoDB"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
homepage = "https://grafbase.com"
repository = "https://github.com/grafbase/grafbase"
keywords = ["dynamodb", "utilities", "grafbase"]

[lints]
workspace = true

[dependencies]
strum = { workspace = true, features = ["derive"] }

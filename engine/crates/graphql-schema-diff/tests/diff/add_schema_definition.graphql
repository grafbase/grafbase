type Kwery {
  ping: Pong
}

enum Pong {
  Yes
  NO
}

type Myutation {
  recordScore(score: Int): Boolean
}

# --- #

type Kwery {
  ping: Pong
}

enum Pong {
  Yes
  NO
}

schema {
  query: Kwery
  mutation: Myutation
}

type Myutation {
  recordScore(score: Int): Boolean
}



[package]
name = "runtime-noop"
authors = ["Grafbase"]
description = "Noop implementations (partial) of Grafbase runtime"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
homepage = "https://grafbase.com"
repository = "https://github.com/grafbase/grafbase"
keywords = ["runtime", "grafbase"]

[lints]
workspace = true

[dependencies]
async-trait = "0.1"
thiserror.workspace = true
runtime.workspace = true
futures-util.workspace = true

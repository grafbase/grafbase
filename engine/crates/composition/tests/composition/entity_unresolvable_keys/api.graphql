type User {
    id: ID!
    posts: [Post!]!
}

type Post {
    comments: [Comment]
    id: ID!
    name: String
}

type Comment {
    content: String
    id: ID!
    postId: ID!
}

type Query {
    getUser(id: ID!): User
}

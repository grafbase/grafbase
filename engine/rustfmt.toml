max_width = 120
edition = "2021"
newline_style = "unix"

# Unstable features, run `cargo +nightly fmt -- --unstable-features` every so often.
imports_granularity = "Crate"
group_imports = "StdExternalCrate"

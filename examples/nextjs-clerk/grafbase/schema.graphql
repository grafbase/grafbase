schema
  @auth(
    providers: [{ type: oidc, issuer: "https://clerk.one.parrot-7.lcl.dev" }]
    rules: [{ allow: private }]
  ) {
  query: Query
}

type TodoList @model {
  id: ID!
  title: String!
  todos: [Todo]
}

type Todo @model {
  id: ID!
  title: String!
  complete: Boolean!
}

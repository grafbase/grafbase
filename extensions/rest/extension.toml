[extension]
name = "rest"
version = "0.2.2"
kind = "resolver"
description = "Declarative REST resolver extension."

[directives]
definitions = "definitions.graphql"
field_resolvers = ["rest"]

[permissions]
network = true

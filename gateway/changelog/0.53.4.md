## Fixes

- Upgrade `cynic` and `reqwest` to lates versions.
- Upgraded GraphQL spec support to latest (via `cynic` upgrade)
- Added support for built-in directives in GraphQL introspection (https://github.com/grafbase/grafbase/pull/3600)

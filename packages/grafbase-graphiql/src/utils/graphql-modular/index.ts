/**
 * Prettify GraphQL query
 *
 * These set of packages were extracted from the graphql-modular repo
 * Atm, they are not published on npm, so we import them manually
 * @see https://github.com/thomasheyenbrock/graphql-modular
 */
import { parse } from './parse'
import { print } from './print'

export { parse, print }

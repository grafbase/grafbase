## Features

- Adds support for federated graphs and subgraphs

## Deprecations

- Deprecates `config({schema: g})` in favor of `config({graph: g})`
- Deprecates the `g` output in favor of `const g = graph.Standalone()`

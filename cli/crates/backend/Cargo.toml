[package]
name = "grafbase-local-backend"
version = "0.9.0"
edition = "2021"
description = "The local backend for grafbase developer tools"
license = "Apache-2.0"
homepage = "https://grafbase.com"
keywords = ["grafbase"]
readme = "README.md"
repository = "https://github.com/grafbase/grafbase"

[dependencies]
common = { package = "grafbase-local-common", path = "../common", version = "0.9.0" }
server = { package = "grafbase-local-server", path = "../server", version = "0.9.0" }
exitcode = "1"
indoc = "1"
thiserror = "1"

query PostCollection($first: Int, $last: Int, $before: String, $after: String) {
  postCollection(first: $first, last: $last, before: $before, after: $after) {
    edges {
      node {
        id
        slug
        author {
          id
          name
        }
      }
    }
  }
}

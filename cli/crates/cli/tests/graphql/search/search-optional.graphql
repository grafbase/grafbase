query OptSearch($query: String!, $limit: Int!) {
  fieldsSearch(query: $query, limit: $limit) {
    id
    ip
    timestamp
    url
    email
    phone
    datetime
    date
    text
    int
    float
    bool
  }
}

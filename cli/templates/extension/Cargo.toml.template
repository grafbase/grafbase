[package]
name = "{{name}}"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
grafbase-sdk = "{{sdk_version}}"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1

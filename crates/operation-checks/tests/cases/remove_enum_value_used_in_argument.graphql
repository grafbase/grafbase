scalar Url

type Query {
  findImage(dominantColor: Color): Url
}

enum Color {
  RED
  GREEN
  BLUE
}

# --- #

scalar Url

type Query {
  findImage(dominantColor: Color): Url
}

enum Color {
  RED
  GREEN
}

# --- #

query findBlueImage {
    findImage(dominantColor: BLUE)
}

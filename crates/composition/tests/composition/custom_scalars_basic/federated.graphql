scalar Yan

scalar Tan

scalar Texture

scalar Tethera

type Sheep {
    # Resolvable in `baz`
    texture: Texture!
    # Resolvable in `foo`
    sheepIdx: Tethera
}

type Query {
    # Resolvable in `foo`
    sheep: Sheep
}

[package]
name = "contracts"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
grafbase-sdk = "0.20.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

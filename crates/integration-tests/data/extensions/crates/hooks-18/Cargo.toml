[package]
name = "hooks-18"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
grafbase-sdk = "0.18"
serde = { version = "1", features = ["derive"] }
